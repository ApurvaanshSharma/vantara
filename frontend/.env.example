# Copy to .env.local for local dev outside Docker.
# Inside Docker, this is set via the NEXT_PUBLIC_API_URL build arg in
# docker-compose.yml instead — Next.js bakes NEXT_PUBLIC_* vars into the
# client bundle at build time, not read at container runtime.
NEXT_PUBLIC_API_URL=http://localhost:8000
